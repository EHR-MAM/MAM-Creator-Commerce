name: Deploy to kiva-1

on:
  workflow_dispatch:
    # Manual deploy only - kiva-1 is behind NAT, no public SSH port
    # To deploy: SSH to kiva-1 and run:
    #   cd /home/kivamaster/projects/mam-platform && git pull && sudo systemctl restart mam-backend

jobs:
  placeholder:
    name: Manual deploy only
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is manual. SSH to kiva-1 and git pull."
